# React-Node-Player-App
React Node JS, web app built without visual studio. Hooked up to Mongodb with batch scripts to handle running, bundling, opening connections in a CLI-only development environment 
